from pathlib import Path
homepath = Path('/content/drive/My Drive/DL project')
modelName = 'convlstm'
code_path = homepath / modelName / '02456_deeplearning_cgmforecast'
data_path = homepath / 'data'
figure_path = homepath / modelName /  'results' / 'cgm_forecast' / 'trained_models'
result_path = homepath / modelName / 'results' / 'cgm_forecast'
model_path = homepath / modelName / 'results' / 'cgm_forecast' / 'trained_models'

